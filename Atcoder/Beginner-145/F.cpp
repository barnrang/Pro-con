#include <iostream> 
#include <algorithm>
#include <cmath>
#include <vector>  
#define ll long long

using namespace std;

int main() {
    int N, K;
    cin >> N >> K;
    ll H[N];
    ll DP[N+1][N+1];

    for (int i = 0; i < N; i++) {
        int tmp;
        cin >> tmp;
        H[i] = tmp;
    }
    for (int i = 1; i < N+1; i++) {
        for (int j = 0; j < N+1; j++) {

        }
    }
}